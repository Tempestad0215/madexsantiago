

export interface linksI{
    first: string | null,
    last: string | null,
    next: string | null,
    prev: string | null,
}

export interface metaI{
    curren_page: number,
    from: number,
    path: string,
    per_page: number,
    to: number
}
